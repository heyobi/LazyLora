# LazyLoRA runtime dependencies.
#
# The engine parses safetensors headers itself and reads tensors with pread, so it
# imports nothing beyond these two. Install torch from the CPU index unless you have a
# CUDA build you want the routed-expert path to use:
#
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
#   pip install -r requirements.txt
#
# torch 2.3 is the floor: the loader converts numpy uint16 arrays to bfloat16 with
# torch.from_numpy(...).view(torch.bfloat16), and from_numpy gained uint16 in 2.3.
# Optional extras live in pyproject.toml: [data] for the tokenizer and dataset scripts,
# [plot] for scripts/plot_proof.py.
numpy>=1.24
torch>=2.3
